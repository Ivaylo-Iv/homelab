apiVersion: v1
kind: Service
metadata:
  name: inbrowser-beatmaker
  namespace: inbrowser-beatmaker
  labels:
    app: inbrowser-beatmaker
spec:
  type: ClusterIP
  selector:
    app: inbrowser-beatmaker
  ports:
    - name: http
      protocol: TCP
      port: 80            
      targetPort: 80      
